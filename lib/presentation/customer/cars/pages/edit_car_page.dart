import 'package:flutter/material.dart';

class EditCarPage extends StatelessWidget {
  const EditCarPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Edit Car Page')));
  }
}
