import 'package:flutter/material.dart';

class PriceCalculator extends StatelessWidget {
  const PriceCalculator({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Price Calculator');
  }
}
