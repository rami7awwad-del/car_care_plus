import 'package:flutter/material.dart';

class MaterialsRequestPage extends StatelessWidget {
  const MaterialsRequestPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Materials Request Page')));
  }
}
