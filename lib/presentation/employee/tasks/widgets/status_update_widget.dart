import 'package:flutter/material.dart';

class StatusUpdateWidget extends StatelessWidget {
  const StatusUpdateWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Status Update Widget');
  }
}
