import 'package:flutter/material.dart';

class CashSettlementPage extends StatelessWidget {
  const CashSettlementPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Cash Settlement Page')));
  }
}
